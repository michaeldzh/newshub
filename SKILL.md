---
name: ai-news-email-push
description: 生成并推送每日 AI 资讯日报。检索过去 24 小时全球 AI 动态（技术/应用/行业），筛选 20 条，强制跨日+本日内去重，经 163 邮箱定时推送。每天北京时间 08:00 自动运行。
---

# 每日 AI 资讯日报（自包含 Skill）

## 功能
1. **generate.py** — 调用大模型 + 联网检索，生成 `AI资讯24小时_YYYY年M月D日.md` 与 `index.html`（20 条，AI 技术 7 / AI 应用 7 / AI 行业动态 6）。
2. **dedup.py** — 去重核心：解析日报、载入全量历史、事件指纹判定、确定性过滤。
3. **check_report.py** — 成稿硬校验（E1–E9），CI 与本地通用。
4. **push_email.py** — 将报告经 163 SMTP 推送（HTML 正文 + Markdown 附件）。

## 运行方式
```bash
pip install -r requirements.txt
export ANTHROPIC_API_KEY=xxx        # 生成报告用
export ANTHROPIC_BASE_URL=...        # 可选
export ANTHROPIC_MODEL=...           # 可选
python generate.py

export REPORT_DIR=.                 # 可选，日报目录（默认脚本所在目录）
python check_report.py              # 成稿校验，FAIL 退出码 1

export NEWS_SMTP_AUTH=xxxx          # 163 授权码（必填）
export NEWS_SMTP_TO=newshub01@163.com
python push_email.py index.html
```

## 定时
通过 GitHub Actions（`.github/workflows/ai-news.yml`）每天**北京时间 08:00**（UTC 00:00）触发；亦可 `workflow_dispatch` 手动触发。密钥全部来自仓库 Secrets，不落盘。

## 去重机制（v2 · 确定性）
> v1 的去重是"软"的：只读最近 3 份日报塞进提示词，且工作流从未把日报提交回仓库，
> 那 3 份永远是 0 份 → 跨日去重完全空转，同一事件隔几天就换个链接重发。v2 改为代码强制。

**三层保障**

1. **全量历史基线** — `dedup.load_history()` 读取报告目录下**全部** `AI资讯24小时_*.md`，
   汇总链接、标题与事件指纹（不再只取最近 3 份）。这条依赖每天把生成的日报
   **提交回仓库**（`generate.commit_report_back()`，写在工作流里会被 403 拒，
   因此放在生成脚本内），缺了它基线就永远是空的。

2. **素材预筛（送稿前）** — `prefilter_materials()` 在提示词之前就用全历史把
   「已覆盖 / 明显超窗」的素材剔掉：链接命中历史链接、标题与历史标题 2-gram ≥0.65、
   事件指纹命中、发布日期早于 `D-2`。判据与下面的闸门**完全同源同阈值**，
   因此不会误杀闸门本会保留的条目，只是把"丢弃"提前到模型挑稿之前。

   > 这条是两次线上事故（2026-09-20、09-23）的根因修复：此前模型每轮都给满 20 条，
   > 但其中 14–15 条是搜索引擎翻出来的老文章（8/18、9/5、9/6 等已发过的），
   > 只能在闸门阶段逐条丢弃 → 去重后仅剩 5–6 条 → 补稿又撞上免费档 429 →
   > 整天不出稿、不推送。

3. **确定性闸门** — `dedup.filter_report()` 在成稿后硬性剔除违规条目，不看模型脸色：
   - `E2` 期内链接重复 → 剔除
   - `E3` 与全部历史链接重复 → 剔除。**不接受 `【进展更新】` 豁免**：链接是文章身份，
     同一条链接被再次使用永远是同一篇文章，不存在"新进展"
   - `E7` 事件链指纹重复（厂商×动作×金额 / 标题 2-gram ≥0.45，期内 + 全部历史）→ 剔除
   - `E9` 标题相似度重复（2-gram Jaccard ≥0.65，**厂商无关**，兜 E7 漏网的"标题里没有厂商名"条目）→ 剔除
   - `E8` 单期同一厂商 >2 条 → 剔除超出部分
   - `E4` 发布日期超出 `[D-1, D]` → 剔除
   - `E5` 缺标题/链接/来源/正文 → 剔除

4. **缺口自愈** — 被剔除的空位按分区向模型追加补稿（最多 3 轮），补稿时附上
   "已保留 + 已剔除 + 往期"的禁用清单。**补稿失败不阻断出稿**（限流/网关抖动等
   瞬时限流会先退避重试 `10/30/60/90` 秒，仍失败则跳过补稿，以现有条数继续）；
   只有首轮生成失败才立即终止（此时没有可发布内容）。

5. **发布门槛：目标 20 条，不足也照发** — 终稿只要 ≥ `MIN_PUBLISH_TOTAL`（默认 1）
   条且无硬性错误，就照常出稿推送，缺口仅打印告警（E1/E6 降级为 WARN）。
   超量（>20）先按分区上限 7/7/6 截尾，不因模型多给两条而空窗。
   `MIN_PUBLISH_TOTAL=1` 表示"只要能出稿就发"，彻底不允许空窗；下限只负责拦住
   「0 条 / 硬错误」这类链路异常 —— 而这时的失败由下面的失败兜底负责送达。

**放行通道**：同一事件若确有实质性新进展（新版本 / 新金额 / 新状态），标题以
`【进展更新】` 开头即可放行 `E7`（事件链）与 `E9`（标题相似度），闸门会降级为 WARN
记录在案。**但它对 `E2`/`E3`（链接重复）无效** —— 复用已发过的链接一律剔除，
标什么都不能例外。禁止用它包装旧闻。

## 失败兜底：不出稿也要送达（v2.1 新增）

> 背景：2026-09-20 与 09-23 各失败一次（去重后条数低于下限），**用户都是几天后才发现**。
> 静默失败比失败本身更贵 —— 一次 429 不该让「今天没有日报」这件事无人知晓。

生成步骤任何形式的失败（素材为空、LLM 失败、条数低于下限、仍有硬错误、未捕获异常）
都不再静默终止，而是落盘 `ALERT.json` + 故障通报页 `index.html`，**并让生成步骤以退出码 0 结束**：

| 通道 | 载体 | 读者 |
| --- | --- | --- |
| 邮件 | `index.html` 作为正文（主题变成 `⚠️ [AI 日报异常] … 未出稿 —— <原因>`），附 `ALERT.json` | 收件箱 |
| GitHub Pages | 同一个 `index.html` 发布到站点 | 打开站点的人 |
| 仓库 | `ALERT.json` 提交入库（下次成功出稿时**自动删除**） | 机器 / 事后追查 |
| Actions 运行页 | 摘要写入 `$GITHUB_STEP_SUMMARY` | 看运行记录的人 |

**为什么生成步骤要「成功」退出**：本机 PAT 缺 `workflow` 作用域，改不了
`.github/workflows/ai-news.yml`（Contents API 与 Git Data API 一律 403），因此加不了
`if: failure()` 步骤；而工作流默认 `if: success()`，生成步骤一失败，
后面的 `Push report to email` 直接跳过 —— 失败就彻底静默。让生成步骤成功，
既有的发信步骤才会执行，通报才能送出去。运行页变绿由 `$GITHUB_STEP_SUMMARY`
与仓库里的 `ALERT.json` 补偿，可见性没有丢失。

**配套**：`check_report.py` 检测到**当日**的 `ALERT.json` 会直接放行（退出码 0）并打印通报摘要 ——
否则这一步会拦下工作流，通报同样发不出去。隔夜残留（日期不是今天）一律不认，
避免用旧故障掩盖今天的真实校验失败。

## 环境变量
| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `ANTHROPIC_API_KEY` | 是 | 生成报告用 |
| `ANTHROPIC_BASE_URL` | 否 | 自定义 API 网关（默认官方） |
| `ANTHROPIC_MODEL` | 否 | 模型名 |
| `TAVILY_API_KEY` | 否 | 检索源；不填则用免 key 的 Google News / HN |
| `REPORT_DIR` | 否 | 日报目录（默认脚本所在目录） |
| `HISTORY_PROMPT_LIMIT` | 否 | 提示词注入的历史条目上限（默认 240） |
| `MAX_REPAIR_ROUNDS` | 否 | 补稿轮数上限（默认 3） |
| `MATERIAL_LIMIT` | 否 | 预筛后送进提示词的素材上限（默认 120） |
| `MIN_PUBLISH_TOTAL` | 否 | 发布下限（默认 1）；不足 20 但 ≥此值即照发，1 = 只要能出稿就发 |
| `MIN_TOTAL` | 否 | `check_report.py` 的发布下限（默认 1），语义同上 |
| `NEWSHUB_ALERT` | 否 | 置 `0`/`false` 关闭失败通报入库（默认开启） |
| `NEWSHUB_COMMIT_BACK` | 否 | 置 `1` 在非 CI 环境下也执行回写（本地调试用） |
| `NEWS_SMTP_USER` | 否 | 发件人，默认 newshub01@163.com |
| `NEWS_SMTP_TO` | 否 | 收件人，默认 newshub01@163.com |
| `NEWS_SMTP_AUTH` | 是 | 163 邮箱授权码 |
